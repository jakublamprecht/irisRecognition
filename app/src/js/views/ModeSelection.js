import React from 'react';
import ModeList from '../components/ModeList';

const ModeSelection = () => {
  return (
    <div className="modeSelection app-view fill-height">
      <ModeList/>
    </div>
  );
};

export default ModeSelection;
